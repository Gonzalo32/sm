{
  "name": "ict-assistant-tradesea",
  "version": "0.1.0",
  "description": "ICT/SMC Visual Assistant Chrome Extension for TradeSea Platform",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build && node -e \"require('fs').copyFileSync('extension/manifest.json', 'dist/manifest.json')\"",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
